local DevTools = script

local createDevTools = require(DevTools.createDevTools)
local getElementName = require(DevTools.getElementName)

return {
	createDevTools = createDevTools,
	getElementName = getElementName,
}
